datasource db {
    provider = "sqlite"
    url      = "file:./prod.db"
}

generator client {
    provider = "prisma-client-js"
    output   = "./prisma/prisma-client-js"
}

model Product {
    id          Int     @id @default(autoincrement())
    name        String
    description String?
    price       Float
}
